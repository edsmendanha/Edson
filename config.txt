# ============================================================
# MetodoBOTDIN - config.txt
# Modelo de configuração. NÃO inclua credenciais reais aqui.
# Copie este arquivo para config.local.txt se quiser,
# ou preencha diretamente antes de rodar (NÃO versione com senha).
# ============================================================

# --- Credenciais IQ Option (PREENCHA ANTES DE RODAR) ---
email = SEU_EMAIL@EXEMPLO.COM
senha = SUA_SENHA

# --- Tipo de conta ---
# demo  = conta de demonstração (recomendado para testes)
# real  = conta real (use com cautela)
tipo_conta = demo

# --- Modo de operação ---
# op  = opções normais (padrão)
# otc = somente OTC (fora de horário de mercado)
# Atenção: OTC e OP NÃO são misturados. O bot respeita esta flag.
modo_operacao = op

# --- Timeframe (em minutos) ---
# Valores suportados: 1, 5
timeframe = 1

# --- Gerenciamento de entradas ---
# Valor por entrada (em USD ou moeda da conta)
valor_entrada = 1.00

# Número máximo de entradas aceitas por sessão (0 = sem limite)
max_entradas = 10

# Stop win: encerra sessão ao atingir este valor de lucro (0 = desabilitado)
stop_win = 0.00

# Stop loss: encerra sessão ao atingir esta perda (0 = desabilitado)
stop_loss = 0.00

# --- Logging ---
# Pasta base para logs (subpastas por data são criadas automaticamente)
log_dir = logs

# Tag opcional para identificar a sessão nos nomes dos arquivos
# Exemplo: session_tag = m1_op  =>  arquivo: signals_2026-03-28_14-30-00_m1_op.csv
session_tag =

# --- Compatibilidade WebSocket ---
# Ative se tiver erro: "on_message() takes 2 positional arguments but 3 were given"
# Recomendado: true para Windows + websocket-client >= 1.7
enable_ws_on_message_patch = true

# --- Diagnóstico ---
# Imprime versão do Python, caminho da iqoptionapi e versão do websocket-client ao iniciar
print_diagnostics = true
